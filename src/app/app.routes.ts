import { Routes } from '@angular/router';
import { Contact } from './page/contact/contact';

export const routes: Routes = [

    {
        path: 'contact',
        component: Contact
    }

    

];
